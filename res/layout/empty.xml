<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_empty"
    style="@style/layoutbg"
    android:background="@drawable/error_bg"
    android:gravity="center_horizontal"
    android:orientation="vertical" >

    <!--
         <ImageView
        android:id="@+id/empty_img"
        style="@style/layout_wrap_all"
        android:src="@drawable/error" />
    -->

    <TextView
        android:id="@+id/tv_noorder"
        style="@style/layout_wrap_all"
        android:layout_marginBottom="@dimen/common_margin"
        android:gravity="center"
        android:text="@string/re_loading_failed" />

    <TextView
        android:id="@+id/tv_joke"
        style="@style/layout_match_wrap"
        android:layout_marginLeft="@dimen/common_margin_double_size"
        android:layout_marginRight="@dimen/common_margin_double_size"
        android:background="@drawable/joke_bg"
        android:padding="@dimen/common_padding"
        android:scrollbars="vertical"
        android:textColor="@color/title_light_gray" />

</LinearLayout>